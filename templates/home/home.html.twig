{% extends 'base.html.twig' %}

{% block body %}
<div class="container text-center">
    <div class="row align-items-start">
        {% for product in products %}
            <div class="col-3">
                {% include 'helpers/userCard.html.twig' %}
            </div>
        {% endfor %}
    </div>
</div>

{% endblock %}
